#!/bin/bash

echo "=========================================="
echo "停止 AI Chat 系统"
echo "=========================================="
echo ""

# 停止前端
echo "1. 停止前端服务..."
pkill -f "vite" && echo "   ✅ 前端已停止" || echo "   ⚠️  前端未运行"
echo ""

# 停止后端
echo "2. 停止后端服务..."
pkill -f "php -S 0.0.0.0:8080" && echo "   ✅ 后端已停止" || echo "   ⚠️  后端未运行"
echo ""

echo "=========================================="
echo "✅ 系统已停止"
echo ""
echo "MySQL 数据库仍在运行（如需停止请执行）："
echo "  docker stop mysql-aiimage"
echo "=========================================="
